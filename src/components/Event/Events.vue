<template>
  <v-container>
    <v-layout row wrap>
      <v-flex xs12 sm10 md8 offset-sm1 offset-md2>
        <v-card class="info" v-for="event in events" :key="event.id">
          <v-container fluid>
            <v-layout row>
              <v-flex xs5 sm4 md3>
                <v-img height="130px" :src="event.imageURL"> </v-img>
              </v-flex>
              <v-flex xs7 sm8 md9>
                <v-card-title primary-title>
                  <div>
                    <h5>{{ event.title }}</h5>
                    <div>{{ event.date }}</div>
                  </div>
                </v-card-title>
                <v-card-actions>
                  <v-btn text color="primary" :to="'/events/' + event.id">
                    <v-icon left light>arrow_forward</v-icon>
                    Подробнее</v-btn
                  >
                </v-card-actions>
              </v-flex>
            </v-layout>
          </v-container>
        </v-card>
      </v-flex>
    </v-layout>
  </v-container>
</template>

<script>
export default {
  computed: {
    events() {
      return this.$store.getters.loadedEvents;
    }
  }
};
</script>
